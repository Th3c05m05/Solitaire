import java.util.*;

public class Main extends Start {

    public static void checkInt() {
        int y=0;
        ArrayList<String>piler = new ArrayList<String>();

        piler.add(deck.get(0));
        int x;
        for (x = 0; x < hand.size(); x++) {

            if(hand.get(x).substring(3,4).equals(piler.get(0)).substring(3,4)){
                piler.add(hand.get(x));
            }

            else if(hand.get(x).substring(3,4,))

        }


    }




    }

